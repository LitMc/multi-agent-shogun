# Windows batch files must use CRLF
*.bat text eol=crlf

# Shell scripts must use LF
*.sh text eol=lf

# Instruction templates and generated outputs must use LF to keep
# Build Instructions Check deterministic across macOS / Linux / Windows.
instructions/*.md text eol=lf
instructions/**/*.md text eol=lf
